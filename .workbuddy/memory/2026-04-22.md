# 2026-04-22
- 自动化执行 `npm run agent:watchlist`：watchlist 9 只基金全部失败，统一原因为缺少大模型 API Key；`data/model-provider-settings.json` 的 `apiKey` 与根目录 `.env` 的 `api_key` 均为空，本轮未更新 `data/analysis-cache/fund-agent-reports.json`，缓存仍停留在 2026-04-20 的上一版结果。
