# 2026-04-27
- 自动化执行 `npm run agent:watchlist`：当前 watchlist 的 9 只基金（007950、023408、018147、025209、017462、004320、016389、013172、270042）全部失败；统一原因为缺少大模型 API Key（`data/model-provider-settings.json` 的 `apiKey` 为空，运行时也未从 `.env` 取到 key）；`data/analysis-cache/fund-agent-reports.json` 未被新结果覆盖，仍保留 2026-04-20 的历史成功缓存。
