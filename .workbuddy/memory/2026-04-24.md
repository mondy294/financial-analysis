# 2026-04-24
- 自动化执行 `npm run agent:watchlist`：当前 watchlist 的 9 只基金（007950、023408、018147、025209、017462、004320、016389、013172、270042）全部因缺少大模型 API Key 而失败；`data/model-provider-settings.json` 中 `apiKey` 仍为空，未写回新的 report/forecast，现有缓存保持不变。
