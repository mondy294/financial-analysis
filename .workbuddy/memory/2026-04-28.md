# 2026-04-28
- 按用户要求将 financial 前端业绩图画布 `.echart-canvas` 高度统一改为 `500px`，并同步覆盖 900px 与 720px 以下的响应式高度，避免移动端回落到 420px/360px。
- 修正业绩图高度未生效问题：`echarts-for-react` 默认给图表容器写入 `300px` 内联高度，会覆盖 CSS class；已在 `src/components/ChartPanel.tsx` 的 `ReactECharts` 上显式设置 `style={{ width: "100%", height: 500 }}`。
