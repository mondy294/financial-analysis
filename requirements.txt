# ============ 运行时依赖 ============
# Python: >= 3.11

# --- 数据源 ---
akshare>=1.15.0                # A 股数据（免费、社区活跃）
requests>=2.32.0

# --- 数据处理 ---
pandas>=2.2.0
numpy>=1.26.0,<2.1.0            # pandas-ta 对 numpy 2.x 兼容仍有问题，锁在 <2.1
pandas-ta>=0.3.14b0             # 技术指标（纯 Python，无编译）
scipy>=1.11.0                   # pandas-ta 隐式依赖，显式声明

# --- 数据库 ---
SQLAlchemy>=2.0.30              # 2.x Mapped 风格 ORM
alembic>=1.13.0                 # DB migration（未来上 PG 时用）

# --- 调度 ---
APScheduler>=3.10.0
pytz>=2024.1

# --- 配置 & 日志 ---
pydantic>=2.7.0                 # 配置校验
pydantic-settings>=2.3.0        # 从 env/toml 加载配置
python-dotenv>=1.0.0
loguru>=0.7.0                   # 简单强大的日志（比标准 logging 上手快）
tomli>=2.0.0; python_version<"3.11"

# --- CLI ---
typer>=0.12.0
rich>=13.7.0                    # 终端美化 & 表格

# --- 可视化 ---
plotly>=5.22.0                  # 交互式图表（用于 HTML 日报）
matplotlib>=3.8.0               # 静态图（用于 Markdown 内嵌）
kaleido>=0.2.1                  # plotly 静态导出

# --- 工具 ---
tenacity>=8.3.0                 # 重试装饰器（akshare 抖动用）
diskcache>=5.6.0                # 磁盘缓存

# --- Web Console ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# --- 可选，未来启用 ---
# faiss-cpu>=1.8.0
# chromadb>=0.5.0
