# 2026-07-15

> 这是 quant_system 项目的每日 memory。项目相关的 memory 全部集中在 `~/Desktop/py/.codebuddy/memory/`，不再写到全局 `~/WorkBuddy/.../memory/`。

## A 股量化分析系统（quant_system）项目启动
- 位置：`~/Desktop/py/quant_system/`（当前 py 目录为空，从零搭建）
- 技术栈锁定：Python 3.11+ / pandas / numpy / akshare / pandas-ta（不用 ta-lib）/ SQLAlchemy + SQLite（预留 PG）/ APScheduler / plotly + matplotlib
- 定位：只做分析和推荐，不做自动交易；每日收盘后离线跑批
- 分层：L0 infra → L1 data(provider+repository) → L2 indicators → L3 strategy → L4 application(selector/backtester) → L5 scheduler → L6 delivery(report)
- 关键决策：
  - provider / repository 严格分离，便于未来接 QMT / tushare
  - 策略输出「hit + reasons + sub_score」，综合评分统一由 scoring 模块算（技术40 / 资金30 / 基本面30，权重走 config）
  - 特征表落地，策略、回测、ML 共用同一份 feature snapshot
  - SQLite 默认，SQLAlchemy 抽象后可切 PG
- 用户新增要求：
  - 股票池配置化，支持 ALL / HS300 / ZZ500 / 自定义
  - 新增 market 模块（index_provider / sentiment / market_features）
  - 新增 feature_store 模块统一管理特征
  - 新增 strategy_signal 表存策略触发历史 + feature snapshot，用于胜率复盘
- 交付节奏：先架构 → 数据库设计 → 项目骨架 → 数据层 → 特征层 → 策略+评分 → 日报+调度

## 数据库设计 v2 调整（用户 review 反馈 id=7qk39）
- daily_feature 基本面冗余字段必须带来源：`financial_snapshot_date` + `financial_ann_date`，回测严格用 ann_date 防未来函数
- strategy_signal 用 `signal_type` (HIT/WATCH/FILTERED/NEAR_MISS) 替代单一 hit bool，便于分析策略失效原因；hit 字段保留但派生自 signal_type
- daily_feature 增加向量预留：`vector_version` + `embedding_id`（不存实体 embedding，未来接 FAISS/Chroma）
- 新增 `backtest_task`（任务配置快照）+ `backtest_result`（收益/回撤/夏普等），与 strategy_performance 分工：performance 是「策略在真实时间轴上的滚动表现」，backtest_result 是「一次实验的完整结果」
- 新增 `data_quality_check` 表记录数据缺失/停牌/异常价格/财务延迟
- ORM 保持数据库中立：sqlite_with_rowid 等特化用 dialect 判断，PG 切换时跳过；不用 SQLite JSON1 语法，只用 SQLAlchemy JSON 抽象
- 用户偏好：分阶段确认，先设计后代码，不接受一次性堆全部实现

## 数据库设计 v2 最终确认点
- signal_record_level 默认 `HIT_ONLY + FILTERED`，WATCH/NEAR_MISS 默认关闭；必须做成 config 配置项，改配置即可开启，业务代码零改动
- data_quality 分级：ERROR→selector 前置剔除；WARN→保留参与评分但日报标注风险；INFO→仅日志
- 硬约束：selector 接收的是**已过滤后的 features**，过滤在 feature_store/selector 前置层完成，策略层保持纯函数不感知质量问题
- strategy_code_hash：对整个 `strategy/` 目录所有 .py 文件的 SHA256 汇总（按路径排序后拼接），任何策略代码变化都能追溯，写入 backtest_task.strategy_code_hash

## 骨架 + 第 4 步确认（用户 review）
- .env.example 默认 QS_STOCK_POOL=HS300（首次验证快），README 生产推荐 ALL
- 股票池必须完全配置化，切换不改代码：ALL / HS300 / ZZ500 / CUSTOM
- CLI 命名保持简洁：init-db / update / select / report / pipeline / backtest / feature / doctor / benchmark
  - feature = 重算特征；doctor = 数据完整性+DB健康检查；benchmark = 策略性能测试
- 依赖注入硬约束：所有业务代码只依赖 Protocol/抽象基类，不 new 具体 Provider/Repository；提供工厂函数按 config 返回实现

## 第 4 步完成情况
- infra 完整落地：logger（loguru）/ cache（diskcache）/ code_hash / db（SQLite PRAGMA 走 connect 事件，dialect 判断，PG 兼容）
- database/models.py：22 张表 ORM 全部实体化，含 ALL_MODELS 断言
- database/migrations.py：create_all + 幂等种子（4 池 + 3 策略 + 16 特征元）+ check_schema_integrity
- data/repository.py：定义 7 个 Protocol（Stock/Kline/Financial/Feature/Signal/Quality/JobLog）+ SQLA 实现骨架 + Repositories bundle + build_repositories(session) 工厂
- data/provider_factory.py：get_stock_provider/get_financial_provider，未来接 QMT 只加分支
- CLI：init-db / doctor / pool list / pool show 已实体化；其余命令保留骨架
- 依赖用双下划线嵌套：`QS_DATABASE__URL` / `QS_STOCK_POOL__POOL`（.env.example 已更新）
- 冒烟通过：init-db 建 22 张表 + 种子成功，doctor/pool list 输出正确
- venv 在 ~/Desktop/py/.venv，已装 sqlalchemy/loguru/pydantic/pydantic-settings/typer/rich/diskcache
- 新增 scripts/smoke_test.sh：一键跑通 init-db → doctor → pool list → 表清单 → 种子数据核对
- 用户体验路径：source .venv/bin/activate → PYTHONPATH=. python -m quant_system.cli xxx；或 pip install -e . 后用 qs xxx 短命令

## 第 5 步范围（用户确认）
- 股票池默认 HS300，切 ALL/ZZ500/CUSTOM 只改 .env（无代码改动）
- kline 起始日期 = 2015-01-01（覆盖 2015 牛 / 2018 熊 / 2020 疫情 / 2021 抱团 / 2022 下跌 / 2023-2025 震荡）
- data_update 严格增量：首次全量 2015-今天；后续只补 last_sync_date+1 → 最新交易日；用 data_sync_state 表做游标
- update 命令拆子命令：stock-basic / stock-pool / kline / financial / market / all（Typer 子命令组）
- provider 层严格 DI：业务用 StockProvider Protocol，不 new AkshareStockProvider
- akshare 调用统一走 tenacity 重试 + diskcache 磁盘缓存 + request_interval_ms 限流

## 第 5 步落地实现
- infra/cache.py：CacheBackend Protocol + DiskCacheBackend + CachePolicy 分层（historical=永久 / recent=1h / realtime=0）+ cached_call 统一入口 + force_refresh
- infra/trading_calendar.py：akshare 拉一次全量交易日永久缓存，bisect 高效查询
- data/stock_provider.py：AkshareStockProvider 用 stock_info_a_code_name 主 + stock_zh_a_spot_em 补市值；kline 走 stock_zh_a_hist 拉两次(raw+hfq)算 adj_factor
- data/financial_provider.py：AkshareFinancialProvider 用 stock_financial_abstract_ths + stock_a_indicator_lg
- data/repository.py：SQLite dialect-aware upsert（on_conflict_do_update）+ read_kline(adj=none/qfq/hfq) 读取层动态复权 + replace_pool_members 带 FK 保护过滤
- data/data_update.py：6 个 Updater + run_update_all；每次都走 job_run_log + data_sync_state
- market/index_provider.py + market/sentiment.py：指数日线 + 涨跌停快照/回填
- CLI 拆分：qs update stock-basic|stock-pool|kline|financial|market|all；qs cache stats|clear|rebuild
- 关键 fix：SQLAlchemy 主键 `BigInteger.with_variant(Integer, "sqlite")`（否则 SQLite BIGINT PK 不自动填充）
- 冒烟：stock-basic 拉了 5528 只入库；HS300 池 298 只（2 只已退市被外键保护过滤）；kline 因东财 API 网络抖动失败，但代码链路完整（tenacity 重试 + 优雅捕获）
- pandas-ta 在 Python 3.11 装不上（新版要求 3.12），第 6 步指标计算需要考虑改用 TA-Lib 或手写 MA/MACD/RSI

## 第 6 步方案（用户确认）
- 指标手写：MA / EMA / MACD / KDJ / RSI / ATR / BOLL / 布林带宽度 / 量比 / N日新高
- **数据层不做板块过滤**：kline 全存；过滤放在 selector 和 backtest 读取时
- 板块配置：`QS_BOARD_FILTER=MAIN`，支持 `MAIN` / `MAIN,GEM` / `MAIN,STAR` / `MAIN,GEM,STAR` / `ALL`
  - MAIN = 600/601/603/605/000/001/002/003（含原中小板）
  - GEM = 300/301（创业板）
  - STAR = 688/689（科创板）
  - ALL 含北交所（8/4/9）
- 首次拉数按 HS300 全部 300 只，不做板块过滤（数据留全）
- 第 6 步交付范围极简：
  - indicators/technical.py：指标计算库
  - feature_store/builder.py：组装 daily_feature 一行
  - data_quality/checker.py：简版巡检（停牌 / 空值率 / 异常涨跌幅）
  - infra/board.py：板块过滤工具函数（未来 selector/backtest 共用）
  - CLI: qs feature / qs quality 命令实体化
- 不做：market_features / factors 单独文件 / feature_store 复杂封装 / vector store 实体

## 第 6 步落地实现
- infra/board.py：classify/parse_board_filter/filter_codes 完整；MAIN 含 600/601/603/605/000/001/002/003
- indicators/technical.py：手写 MA/EMA/MACD/KDJ/RSI/ATR/BOLL + 量比 + N 日新高 + 综合 compute_all
- feature_store/builder.py：build_feature_row 单只 + build_features_for_date 批量；含 financial_snapshot_date + financial_ann_date 血缘
- data_quality/checker.py：MISSING_KLINE / SUSPENDED / ABNORMAL_PRICE / FEATURE_NULL_RATE_HIGH / SYNC_STALE；get_blacklist_for_selector 提供给 selector 前置过滤用
- data/repository.py：新增 FeatureRepository（upsert_features / read_features_on / count_features_on）
- CLI: qs feature / qs quality 实体化；doctor 加上 Board Filter 显示
- 默认 kline 起始日改为 2015-01-01
- scripts/seed_mock_kline.py：网络不通时的合成数据播种，20 只股票 × 369 交易日 = 7380 条
- 冒烟通过：feature 输入 298 只 → 有 kline 的 20 只成功；quality 生成 278 条 MISSING_KLINE WARN；板块过滤 filter_codes 输出正确

## 第 7 步完成情况（策略 + 评分 + 日报）
- strategy/base_strategy.py：BaseStrategy 抽象 + StrategyResult dataclass（signal_type + sub_score + reasons + category）
- 三个策略：BreakoutStrategy / MomentumStrategy / ValueGrowthStrategy，均是纯函数，输入 DataFrame 输出 result 列表
- strategy/scoring.py：score_stock 综合评分（技术 40 + 资金 30 + 基本面 30 + 多命中加成）；权重全走 config
- strategy/stock_selector.py：run_selector 编排（板块过滤 → DQ 黑名单 → 3 策略 → 综合评分 → TopN → 写 strategy_signal）；写库幂等（先 DELETE）；signal_record_level 从 config 读
- report/charts.py：plotly K 线迷你图（红涨绿跌，MA20）；plotly.js 走 CDN，HTML 不含 JS 体积
- report/daily_report.py：Markdown（纯文本，终端友好）+ HTML（内嵌 CSS + plotly chart）双输出；同时落 daily_report / daily_report_item 表；股票中文名自动 join
- CLI: qs select / qs report / qs pipeline 全部实体化；pipeline 5 步依次跑；--skip-update 支持
- 冒烟：mock 20 只 → 命中 600019.SH（趋势）+ 002050.SZ（突破），生成 HTML 29KB 含 2 张 plotly K 线图，md/html/db 三份产物齐全
- 装了 plotly（`pip install plotly`）
- market_daily 首次只拉当日快照，历史通过 `qs update market --backfill` 独立回填
- 复权方案：daily_kline 只存原始价 + adj_factor；repository 提供 read_kline(adj="none|qfq|hfq")
- akshare 缓存分级 + 全局强制统一：
  - 历史（trade_date < today-30）：TTL=None 永久
  - 近 30 交易日：TTL=1h
  - 当日快照（realtime）：不缓存
  - Provider 层禁止自己写缓存逻辑，必须通过 `infra/cache.py` 的 cached_call() 统一入口
  - 提供 `qs cache clear` / `qs cache rebuild` / `force_refresh=True` 参数
